# Provider credentials (values must remain local and must never be committed in .env)
XIYOU_API_KEY=
SORFTIME_API_KEY=

# Environment-owned API origins; no production address is compiled into the clients
XIYOU_API_BASE_URL=
SORFTIME_API_BASE_URL=
